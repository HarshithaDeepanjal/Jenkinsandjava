name: helloworld
on: 
  push:
    branches:
       -main
    jobs:
    first-helloworld-job:
    runs on: ubuntu-latest
    steps:
      -name: Checkout code
      uses: actions/checkout@v3
      -name: Print message
      run: echo "hello world"
